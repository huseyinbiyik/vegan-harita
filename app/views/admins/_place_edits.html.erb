<% if user.change_logs.any? %>
  <thead>
    <tr>
      <th>Öneri Tarihi</th>
      <th>Mekanın Adı</th>
      <th>Adres</th>
      <th>Vegan mı?</th>
      <th>Instagram</th>
      <th>Facebook</th>
      <th>Twitter</th>
      <th>Web Sitesi</th>
      <th>E-posta</th>
      <th>Telefon</th>
    </tr>
  </thead>
  <tbody id="place_edits">
    <% user.change_logs.each do |place_edit| %>
      <tr>
        <td><%= place_edit.created_at %></td>
        <td><%= place_edit.changeable.name %></td>
        <td><%= place_edit.changeable.address %></td>
        <td><%= place_edit.vegan %></td>
        <td><%= place_edit.instagram_url %></td>
        <td><%= place_edit.facebook_url %></td>
        <td><%= place_edit.twitter_url %></td>
        <td><%= place_edit.web_url %></td>
        <td><%= place_edit.email %></td>
        <td><%= place_edit.phone %></td>
      <tr>
        <td><%= place_edit.updated_at %></td>
        <td><%= place_edit.name %></td>
        <td><%= place_edit.address %></td>
        <td><%= place_edit.vegan %></td>
        <td><%= place_edit.instagram_url %></td>
        <td><%= place_edit.facebook_url %></td>
        <td><%= place_edit.twitter_url %></td>
        <td><%= place_edit.web_url %></td>
        <td><%= place_edit.email %></td>
        <td><%= place_edit.phone %></td>
      </tr>
    <% end %>
  </tbody>
<% else %>
  <p>No place edits found for this user.</p>
<% end %>
