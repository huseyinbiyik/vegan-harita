<% places.each do |place| %>
  <div class="search-results-link-container">
    <%= link_to highlight(place.name, params[:name_search]),
    place,
    class: "search-results-link" %>
  </div>
<% end %>
